package com.example.chatbot.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // Add additional web config if needed (CORS/global interceptors) 
}
